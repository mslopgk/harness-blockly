# 인수인계 — Dobot 제어 라이브러리 · AI+로보틱스 교육 스택

- 작성: 2026-09-08 / 이지호 (sh.cho@pusan.ac.kr)
- 원본 작업 경로(내 PC): `C:\Users\user\busan-robotics` — 이 폴더는 그 **복사본**이다.
- 이 문서의 목적: **코드만 봐서는 알 수 없는 것**(왜 그렇게 만들었는지, 무엇을 약속했는지, 어디서 데였는지)을 남긴다.
- 표기: 내가 직접 확인한 사실은 그냥 쓰고, 기억·정황으로 미루는 것은 **(추정)** 이라고 붙였다.
- 자격증명은 **값을 쓰지 않았다.** 어느 파일에 있는지만 적었다(§3.6).

---

## 1. 무엇인가 / 어디까지 됐나

### 1.1 무엇인가

국립부산과학관 용역 **"2026 AI+로보틱스 교육 플랫폼 구축 및 프로그램 개발"** 의 산출물 일체다.
과업지시서 원문이 `_과업지시서_PM/국립부산과학관/` 에 hwp·pdf·md 세 형태로 들어 있다 — **분쟁이 생기면 이게 기준 문서다.**

세 갈래가 한 폴더에 같이 있다. 서로 물려 있어서 떼면 이해가 안 된다.

| 갈래 | 위치 | 한 줄 설명 |
|---|---|---|
| ① 교육용 플랫폼 (BlockPy) | `플랫폼개발/blockly프로젝트` | 블록 ⇄ 파이썬을 **무손실 양방향** 변환하는 학생용 프로그램. Electron 데스크톱 설치본까지 만들었다. |
| ② 강의자료 | `강의자료개발` | 중등·고등 차시별 수업자료. **종이 인쇄용 + 웹 교재** 두 형태를 같은 원본에서 생성한다. |
| ③ 로봇 제어 라이브러리 | `플랫폼개발/자체라이브러리/dobotkit` | Dobot Magician Lite / Magician GO 를 학생이 쓸 수 있게 감싼 파이썬 패키지. **PyPI 배포됨(dobotkit 0.2.0).** |

②의 수업이 ①에서 돌아가고, ①의 예제가 ③을 호출한다. 그래서 셋 중 하나만 고치면 어긋난다.

### 1.2 어디까지 됐나

**① BlockPy — 동작함. 수업에 쓸 수 있는 상태.**

- CPython 3.12 `ast` 기반 IR 파이프라인으로 변환 코어 완성. 왕복 계약은 `ast.dump(원본) == ast.dump(재생성)`.
- 2026-08-11 에 **수업용 개선 8건**을 넣고 커밋했다(§6 끝 목록). 마지막 커밋 `e3ae3db`.
- **온라인 설치본** `BlockPy-Setup-1.0.0.exe` 완성 — 설치 시 파이썬·opencode·DobotLink·PyPI 패키지를 인터넷에서 받아 설치한다. 실행 확인했다.
- IR 게이트 186 spec 통과(1건은 플레이키, §5.7).

**② 강의자료 — 중등은 완료, 고등은 미완.**

- **중등 2차시(가위바위보)**: 파이썬을 **완전히 걷어내고 블록 전용으로 전면 개편**했다. 31개 파일 + 생성 슬라이드 73장에서 위반 0건 확인. 종이용/웹 두 버전 다 실물 검증. Codex + Claude **2중 검수** 마쳤다(`강의자료개발/_공용/검수결과_2중검수_20260811.md`).
- **중등 1차시(AI 분리수거)**: 자료 존재. 시범수업 자료도 있음(`_시범수업_20260825`).
- **고등**: 자료가 **1차시(자연어제어 VLA)·2차시(자율주행 비전AI) 두 개뿐**이다. 프로그램 쪽 예제는 고등 3차시분(자연어제어·텔레오퍼레이션·자율주행)이 다 있는데, **텔레오퍼레이션 차시의 강의자료 폴더가 없다.** `텔레오퍼레이션_standalone.py` 가 엉뚱하게 `수업_중등2차시_.../소스코드/` 안에 들어 있다. (추정) 착수 전이거나 폴더 배치가 어긋난 상태다. → §6-1.
- 전달본 패키지: `_전달_중등_20260811/` (종이·웹·프로그램 3종 + `00_먼저_읽어주세요.md`). **이게 과학관에 넘긴 형태다.**

**③ dobotkit — 배포됨.**

- 팔(MagicianLite) 제어를 **DobotLink RPC 경로로 전면 재작성**했다. 시리얼/DLL 직접 접근 안 한다. 전 기능 실기 검증 후 master 병합.
- robotSpecs 21개 API 전부 PyPI 0.2.0 에 존재하는지 확인했다.

---

## 2. 파일·폴더 지도

### 2.1 최상단

| 경로 | 용량 | 무엇 | 후임이 볼 순서 |
|---|---|---|---|
| `HANDOVER.md` | — | **이 문서** | 1 |
| `_과업지시서_PM/` | 1MB | 과업지시서(hwp/pdf/md) + 스프린트 계획 엑셀 | 2 — 무엇을 약속했는지 |
| `플랫폼개발/` | 1.6GB | 프로그램·라이브러리 소스 전부 | 3 |
| `강의자료개발/` | 240MB | 차시별 수업자료 원본 | 4 |
| `_전달_중등_20260811/` | 1.6GB | **과학관에 넘긴 중등 전달본**(종이·웹·exe) | 5 — 결과물 실물 |
| `_보관/` | 3.3GB | 빌드 산출물·백업·중복본. **하나도 지우지 않고 옮겨만 뒀다** (§7) | 필요할 때만 |
| `.claude/` `.agents/` `skills-lock.json` | 1MB | AI 에이전트 작업 설정 | 참고 |

### 2.2 `플랫폼개발/`

| 경로 | 용량 | 무엇 |
|---|---|---|
| `blockly프로젝트/` | 58MB | **BlockPy 본체.** git 저장소(§3.5). 여기가 핵심이다. |
| `커리큘럼코드/` | 1.5GB | 과업 산출 코드 3묶음. `taskA_platform`(플랫폼), `taskB_curriculum`(커리큘럼 — **이 안 `models/` 1.5GB 가 용량 대부분**), `taskC_seminar`(세미나). `qa/` `pm/` `PROGRESS.md` `작업완료_보고.md` `강사_온보딩_가이드.docx` 동봉 |
| `벤더참고/` | 37MB | Dobot 공식 매뉴얼 PDF, `pydobot-master`, K210 카메라 실험 스크립트 |
| `자체라이브러리/` | 5MB | `dobotkit`(배포본 소스) · `dobotkit-gui` · `magiciango_go` |
| `API문서/` | 1MB | `두봇_라이트_API.md` · `두봇_고_API.md` — **한국어로 정리한 API 표. 실무에서 제일 자주 열어 본 문서다.** |

`커리큘럼코드/taskB_curriculum/models/` 안의 `Qwen3VL-2B-Instruct-Q4_K_M.gguf`(1.1GB) + `mmproj-…Q8_0.gguf`(445MB) + `hand_landmarker.task` + `llamacpp/` 는 **빌드 산출물이 아니라 실제로 쓰는 모델 파일**이라서 `_보관` 으로 옮기지 않았다. 지우면 고등 VLA 차시가 안 돈다.

### 2.3 `blockly프로젝트/` 안에서 꼭 알아야 할 곳

| 경로 | 무엇 | 왜 중요한가 |
|---|---|---|
| `README.md` | 2026-08-11 전면 재작성 | 수업용 기능 + 온라인 설치본 기준으로 다시 씀. 최신이다. |
| `CLAUDE.md` / `AGENTS.md` | 아키텍처·규칙 문서 | **손대기 전에 읽어라.** 하드 제약(이름 테이블 금지 등)이 여기 적혀 있다. |
| `DOCS/인수인계_변경점_20260811.md` | 462줄 | **원저작자(shain1912) 버전 대비 무엇이 바뀌었는지** 기능·기술 전부 정리. 넘겨줄 때 이 문서를 같이 준다. |
| `DOCS/개선계약_20260811.md` | 개선 8건의 요구사항 원문 | 왜 그렇게 만들었는지의 근거 |
| `src/utils/*.js` (`pyAstBridge.js` 등) | 변환 코어 | `window.*` 전역 + side-effect import 구조 |
| `src/data/robotSpecs.json` `tmSpecs.json` `visionSpecs.json` | 라이브러리 introspection 결과 | 블록 목록의 원천. 손으로 이름 테이블 만들지 말 것 |
| `server.js` | Express 백엔드(:3001) | 127.0.0.1 만 바인딩. 파이썬 실행·pip·파일 접근을 무인증으로 노출하므로 **절대 외부에 열지 마라** |
| `runtime/tm.py` `runtime/robotvision.py` | 파이썬 네이티브 TM · 비전 | `to_robot(u,v)` 가 카메라 픽셀 → 로봇 mm 변환 |
| `electron/` | 데스크톱 셸 + `setup/` 온라인 설치 로직 | `manifest.json`(받을 목록) `download.cjs` `install.cjs` `window.cjs` |
| `public/examples/` | 수업 예제 15개(단계별) | `m1_1_찾기.py` … `h3_3_달리기.py` |
| `agent-context/AGENTS.md` | 학생용 AI 도우미가 읽는 그라운딩 문서 | 도우미가 로봇 API를 알게 하는 근거 |
| `tests/ir_*.spec.js` | 병합 게이트 | 여기 깨지면 병합하지 마라 |

### 2.4 `강의자료개발/`

| 경로 | 용량 | 무엇 |
|---|---|---|
| `수업_AI분리수거_로봇팔/` | 70MB | 중등 1차시 |
| `수업_중등2차시_가위바위보_핸드트래킹/` | 65MB | 중등 2차시 — **블록 전용 개편 완료본** |
| `수업_고등1차시_자연어제어_VLA/` | 9MB | 고등 1차시 |
| `수업_고등2차시_자율주행_비전AI/` | 32MB | 고등 2차시 |
| `_공용/` | 17MB | **생성기와 공용 자산.** `print.css` `interactive.js` `build_prints.py` `render_slides.py` `packs/` — 여기를 고치면 모든 차시가 같이 바뀐다 |
| `_공통문서/` | 1MB | 지도안·강의계획서·`docs/adr/` **결정 기록**(예: `0004-중등-블록전용-파이썬-금지.md`) |
| `_시범수업_20260825/` | 11MB | 2026-08-25 시범수업 실물 자료 |
| `_전달_클로드디자인_중등/` | 34MB | 디자인 시안 왕복본 (추정) |
| `_분석/` | 4MB | 사전 분석 메모 |
| `수업포털.html` `.pdf` | 1MB | 차시 모음 포털 |
| `차시별_수업흐름_요약.pptx` | 1MB | 흐름 요약 |

각 차시 폴더 구조는 대체로 `문서/` `사이트/` `슬라이드/` `인쇄물/` `소스코드/` 다.
**`슬라이드/_source/gen_*.py` 가 원본이고 `슬라이드/slide_NN.html` 은 생성물이다.** 생성물을 고치면 다음 생성에서 날아간다(§5.4).

---

## 3. 실행·빌드·배포 절차 (실제로 쓰던 명령)

### 3.1 BlockPy 개발 서버

```bash
cd 플랫폼개발/blockly프로젝트
npm install
npm run vendor      # 필수. public/vendor 는 gitignore 라서 이걸 안 하면 Blockly 가 안 뜬다
npm start           # 프런트 Vite :3000 + 백엔드 Express :3001 동시 기동
```

- `npm run dev` = 프런트만, `npm run server` = 백엔드만.
- Vite 가 `/api/*` 를 :3001 로 프록시한다. Pyodide 의 SharedArrayBuffer 때문에 COOP/COEP 헤더를 붙여 서빙한다.
- 포트 바꾸려면 `PORT` 환경변수 (playwright 도 이걸 따른다).

### 3.2 테스트 (병합 게이트)

```bash
npm run test:ir     # 186 spec — ★ 백엔드(:3001)가 떠 있어야 한다. 3개 spec 이 /api 를 친다
npm run test:gen    # blockpy-gen 단위테스트 (node --test)
npm test            # 전체 Playwright
npm run test:report # HTML 리포트
```

### 3.3 데스크톱 설치본

```bash
npm run electron:dev                       # 개발 셸
node scripts/build-desktop.cjs             # 실제로 쓴 빌드 스크립트
npm run dist -- --config.npmRebuild=false  # ★ npmRebuild=false 없으면 실패한다 (§5.1)
```

- `build-desktop.cjs` 는 `~/blockpy-release` 에 스테이징한다 — 프로젝트 폴더에서 바로 패키징하면 Windows EPERM 이 난다.
- 결과물 `BlockPy-Setup-1.0.0.exe` (NSIS). **온라인 설치본**이므로 설치 시 인터넷이 필요하다.
- 설치본이 웹에서 받는 것: 파이썬 임베드, opencode, DobotLink 설치본(Dobot CDN), PyPI 패키지(`dobotkit`, `opencv-python`, `tensorflow` 등 — 버전 고정). 목록·해시는 `electron/setup/manifest.json`.

### 3.4 강의자료 생성

```bash
cd 강의자료개발
python _공용/render_slides.py                          # 슬라이드 HTML 생성
python _공용/build_prints.py                           # 종이 인쇄용(PDF) 생성
python 수업_중등2차시_.../슬라이드/_source/gen_m2.py    # 차시별 슬라이드 생성기
```

정확한 인자 조합은 각 스크립트 상단 주석을 보라. **(추정)** 나는 주로 위 순서로 돌렸다.

### 3.5 git 저장소

```
origin    https://github.com/mslopgk/harness-blockly.git   (내가 쓰던 곳, master 최신)
upstream  https://github.com/shain1912/Harness_Blockly.git (원저작자)
```

- 현재 브랜치 `master`, HEAD `e3ae3db`. 작업트리 깨끗함. **feature 브랜치는 정리해서 삭제했다.**
- `자체라이브러리/dobotkit`, `dobotkit-gui`, `magiciango_go` 도 각각 별도 git 저장소다(중첩 `.git` 이 있다 — 서브모듈이 아니라 폴더째 들어 있다).

### 3.6 환경변수 · 자격증명 (값은 여기 적지 않는다)

| 이름 | 용도 | 값이 어디에 있나 |
|---|---|---|
| `DEEPSEEK_API_KEY` | 학생용 AI 도우미(opencode) | `플랫폼개발/blockly프로젝트/electron/bundled-keys.cjs` (647B). **저장소에는 gitignore 되어 커밋 안 됨** |
| AI 키(앱 설정 저장분) | 앱 내 Settings 에서 저장한 키 | `플랫폼개발/blockly프로젝트/blockpy-config.json` (152B). 역시 gitignore |
| `MINIMAX1`..`MINIMAX4` | 변환 보조 AI(MiniMax) | `.env` — **이 복사본에는 없다.** 필요하면 새로 발급 |
| `BLOCKPY_WORKSPACE` | 학생 파일 루트 / 실행 cwd | 기본값 `~/BlockPyWorkspace` |
| `PYTHON_CMD` | 사용할 파이썬 | 패키징본에서는 `electron/main.cjs` 가 번들 파이썬으로 지정 |
| `BLOCKIFY_ALLOW` | `/api/blockify` 가 import 해도 되는 모듈 제한 | 미설정 시 제한 없음 — **import 는 top-level 코드를 실행한다.** 운영에선 좁혀라 |
| 서버·SSH 자격증명 | (이 프로젝트는 서버 배포 없음) | 인수인계 패키지 `30_계정_API키/02_환경변수_값.md`, `30_SSH접속키/` |

**키를 새로 넣을 때:** 위 두 파일은 저장소에 커밋되지 않는다. 다른 PC 에서 빌드하려면 그 PC 에 직접 만들어야 한다. 값을 메신저·이슈에 붙이지 마라.

### 3.7 실행에 필요한 하드웨어

| 무엇 | 조건 |
|---|---|
| 로봇팔 Magician Lite | **DobotLink 가 실행 중이어야 한다.** 안 켜면 아무 명령도 안 먹는다 |
| 카메라 | 일반 USB 웹캠. 카메라 번호를 자주 틀린다(§5.5) |
| Magician GO (주행) | 고등 3차시용 |
| 서버 배포 | **없다.** 전부 로컬 실행이다 |

---

## 4. 상대 기관 · 담당자 · 약속

### 4.1 상대

| 상대 | 무엇 | 비고 |
|---|---|---|
| **국립부산과학관** | 발주처 | 과업지시서: `_과업지시서_PM/국립부산과학관/3. 과업지시서_2026 AI+로보틱스 교육 플랫폼 구축 및 프로그램 개발 용역 (1).hwp` (+pdf, +`과업지시서.md`) |
| **shain1912** (GitHub) | BlockPy 원저작자 | **이 프로젝트를 이 사람에게 넘기기로 이야기가 되어 있었다.** 넘길 때 `DOCS/인수인계_변경점_20260811.md` 를 같이 준다 |
| 코드코리아 | 수행사 | 사업자 정보는 인수인계 패키지 `20_계약_회사서류/` |

**과학관 실무 담당자 성명·연락처는 이 폴더와 내 작업 기록에 남아 있지 않다.** 계약서(`20_계약_회사서류/`)와 스프린트 엑셀(`_과업지시서_PM/dobot_sprint_plan_post_platform_Jun-Dec2026.xlsx`)에서 확인하라. 모르는 것을 여기 적지 않는다.

### 4.2 지켜야 할 약속 — 어긴 결과가 곧 사고다

1. **중등은 파이썬을 아예 보여주지 않는다.** 블록 전용이다. 근거: `_공통문서/docs/adr/0004-중등-블록전용-파이썬-금지.md`. 예제 파일·화면 캡처·터미널 출력 전부 포함이다. (터미널이 프로그램 전문을 찍어서 이걸 어긴 적이 있다 — §5.6)
2. **중등 2차시에서 로봇은 이동하지 않는다.** 제자리에서 집게만 여닫는다. 교사가 소리 내어 읽는 안전 대본에 그렇게 적혀 있다("오늘 로봇은 제자리에서 집게만 여닫습니다"). 그래서 2차시는 **캘리브레이션도 필요 없다.** 예제에 `move_to` 를 넣으면 안전 약속을 깨는 것이다.
3. **중등 1차시 하드웨어는 집게(그리퍼)다.** 흡착이 아니다. 준비물 점검표에 "집게가 단단히 달려 있다"가 있다. 코드는 `grip`, `suck` 아니다.
4. **AI 생성 이미지 금지.** 강의자료에 넣지 않는다.
5. **`{플랫폼}` 표기 규칙**을 강의자료에서 지킨다(제품명을 박지 않고 치환 표기).
6. **코드 마지막에는 항상 펌프를 끈다.** 안 끄면 계속 돌아간다. 모든 예제 끝에 `팔.pump_off()` 가 있다.

---

## 5. 함정과 주의사항 (내가 실제로 데인 것들)

### 5.1 빌드 · 패키징

- **`npm run dist` 는 `--config.npmRebuild=false` 없이는 실패한다.** node-pty 리빌드 중 winpty `GetCommitHash.bat` 을 못 찾는다. 근데 **리빌드 자체가 불필요하다** — node-pty 1.1.0 은 node-addon-api(N-API)라서 prebuilt 바이너리가 Electron 과 ABI 호환이다. 같이 `!**/*.pdb` 제외도 넣었다.
- **`npm run vendor` 를 안 하면 화면이 빈다.** `public/vendor` 는 gitignore 다. 새 PC 첫 세팅에서 반드시.
- **패키징은 `~/blockpy-release` 에서 해라.** 프로젝트 폴더에서 바로 하면 Windows EPERM.

### 5.2 임베드 파이썬 (`python-embed`)

- **`._pth` 파일은 BOM 없는 ASCII 여야 한다.** PowerShell `-Encoding utf8` 이 BOM 을 붙여서 `ModuleNotFoundError: encodings` 로 파이썬이 아예 안 뜬 적 있다.
- **`._pth` 가 있으면 isolated mode 라 `PYTHONPATH` 를 무시한다.** 그래서 `import tm` 이 패키징본에서만 깨졌다. `_blockpy_pythonpath.pth` 훅으로 되살렸다. 이 파일 지우지 마라.

### 5.3 온라인 설치본 다운로더

- 예전에 **7바이트 붙은 손상 파일이 검증을 통과했다**(해시 없고 1KB 오차 허용이었음). 지금은 해시 고정 + 해시 없으면 크기 정확 일치 + 둘 다 없으면 재사용 안 함으로 조여 놨다. **허용 오차를 다시 넓히지 마라.**
- 받는 곳 중 **Dobot CDN 이 제일 약한 고리다.** PyPI·npm 은 안 죽지만 Dobot 쪽은 언제든 URL 이 바뀔 수 있다. 설치 실패 신고가 오면 여기를 먼저 의심하라.
- `manifest.json` 에 항목이 빠지면 `undefined.label` 로 죽는다 → `need(id)` 가드가 있다.

### 5.4 강의자료 생성 파이프라인

- **`print.css` 안에 `*/` 를 만들 수 있는 문자열을 쓰지 마라.** 주석 안에 `수업_*/사이트/*.html` 를 적었더니 `*/` 가 주석을 조기 종료해서 **`@media print` 블록 전체가 무효화**됐다. PDF 가 계속 US-Letter 로 나오는데 원인을 못 찾아 두 갈래로 따로 검증해서야 찾았다. 파일 맨 위에 경고를 달아 뒀다.
- **생성물을 고치지 말고 생성기를 고쳐라.** `slide_07.html` 을 직접 고쳤다가 다음 생성에서 날아갔다. 원본은 `_source/gen_*.py`.
- **f-string 안에 큰따옴표 넣지 마라.** `style="white-space:nowrap"` 을 이중따옴표 f-string 에 넣어 `gen_m2w.py` 가 깨졌다. 생성기 고친 뒤엔 4개 전부 문법 검사해라.
- 웹판은 인쇄하면 내용이 조용히 사라진다 — `interactive.js` 의 `beforeprint` 훅이 퀴즈·핫스팟·탭 패널을 펼쳐 준다. 이 훅이 없으면 퀴즈 7문항 중 6문항과 안전 대본이 통째로 인쇄에서 빠진다. **건드리지 마라.**
- `interactive.js` 의 `normSamples()` 는 한국어 키(`이름표`/`확신`)를 받는 알리아스다. 데이터 키를 영어로 되돌리려면 여기도 같이 봐라.

### 5.5 프로그램 · 수업 운영

- **AI 도우미가 카메라 번호를 자주 틀린다.** 그래서 `/api/cameras` 와 실시간 상태(`/api/ui-state`)를 붙여 도우미가 현재 상황을 보게 했다. 그래도 완전하지 않다.
- **컨트롤러에 알람이 뜨면 모션이 전부 막힌다. 전원 리셋 말고는 안 풀린다.** 수업 중에 이러면 시간을 다 잡아먹는다. 예비 시간을 두라.
- **DobotLink 를 켜지 않으면 아무것도 안 된다.** 학생 PC 점검 1번 항목.
- **TensorFlow 는 자동으로 안 깔려서** 설치본이 `pip install tensorflow` 를 하게 만들었다. 느리다 — 설치 진행 상황은 `/api/tm/prepare-status` 로 폴링한다(2초). 이 폴링을 빼면 TM 저장이 최대 30분 동안 조용히 멈춘 것처럼 보인다.
- **브라우저 티처블머신 모델과 파이썬 TM 은 호환되지 않는다.** 브라우저는 mobilenet, 파이썬은 keras 계열이라 같은 사진에서 코사인 유사도가 0.79 밖에 안 나온다. **브라우저에서 만든 모델을 파이썬에서 재사용하게 안내하지 마라.**
- 카메라↔로봇 좌표 변환은 아핀이다: `x = M[0][0]*u + M[0][1]*v + M[0][2]`, `y = M[1][0]*u + M[1][1]*v + M[1][2]`. **분류 수업(중등 1차시)은 이 보정값이 있어야 "어느 자리에 있어도 찾아가 집는다"가 성립한다.** 로봇 패널 → 자리 맞추기를 미리 해 둬야 한다.

### 5.6 코드 규칙 (어기면 리뷰에서 막힌다)

- **라이브러리 이름 하드코딩 테이블 금지.** introspection(`/api/blockify` + Jedi `/api/infer-types`)으로 런타임에 해결한다. `libraryAbstraction.js` 의 cv2 `AI_PRESETS` 만 예외로 살아 있는데 **확장하지 마라.**
- **`blocklyToIr` 는 `ir_*` 아닌 블록을 만나면 예외를 던진다.** 조용히 넘기지 않는 게 의도다.
- **왕복 손실이 생기면 필드 키 이름 불일치를 먼저 의심하라.** `irToBlockly`(쓰기)와 `blocklyToIr`(읽기)의 `extraState` 키가 정확히 같아야 한다.
- **`server.js` 는 127.0.0.1 전용이고 Host 헤더도 검사한다.** 무인증으로 파이썬 실행·pip·파일 접근을 노출하므로 절대 밖으로 열지 마라. "개발 편의"로 0.0.0.0 바인딩하지 마라.
- **터미널에 프로그램 전문을 찍지 마라.** `App.jsx` 가 실행마다 코드 전체를 출력해서 중등 학생이 파이썬을 매번 봤다(§4.2-1 위반). 줄 수만 찍게 바꿨다.
- 학생 워크스페이스에 임시 spec·산출물을 남기지 마라. 한 번 7개가 남아 있었다.
- `package.json` 은 MIT 라고 선언하지만 **LICENSE 파일이 실제로 없다.** README 에서 링크 걸지 마라(고쳐 뒀다).

### 5.7 테스트 · 도구

- **`npm run test:ir` 은 백엔드가 떠 있어야 한다.** 3개 spec 이 `/api` 를 친다. 백엔드 없이 돌려서 "회귀"로 착각하지 마라.
- **`ir_desugar_app.spec.js` 는 플레이키하다.** 전체 실행에서 한 번 실패하고 단독·재실행에서 통과했다(186/186). 한 번 실패했다고 되돌리지 마라.
- **한글 든 `.ps1` 은 UTF-8 BOM 으로 저장해야 한다.** 아니면 PowerShell 5.1 이 CP949 로 읽어 파서 오류가 난다.
- **heredoc 에서 백슬래시가 접힌다.** `\\` 가 `\` 로 두 번 접혀서 JSON·JS 가 깨진 적 있다. 큰 JS/JSON 은 heredoc 대신 에디터로 쓰고, 경로는 슬래시(`/`)를 쓰고, 개행은 `String.fromCharCode(10)` 로 피했다.
- `npx shadcn@latest mcp` 는 도구 초기화를 무한 정지시킨다(이 프로젝트와 직접 관련은 없지만 같은 PC 환경의 공통 함정).

---

## 6. 다음에 해야 할 일

우선순위 순.

1. **고등 텔레오퍼레이션 차시 강의자료 만들기.** 프로그램 예제(`h2_1_배우기.py` `h2_2_움직이기.py` `h2_3_조종하기.py`)와 `텔레오퍼레이션_standalone.py` 는 있는데 `강의자료개발/` 에 해당 차시 폴더가 없다. 중등 2차시 폴더 구조를 그대로 본떠서 만들면 된다. (추정) 미착수 항목이다.
2. **고등 자료를 중등과 같은 수준으로 끌어올리기.** 중등만 종이+웹 2종·2중검수를 마쳤다. 고등 1·2차시는 구성이 고르지 않다(고등 1차시엔 `슬라이드/` 가 없다).
3. **shain1912 에게 인계.** `DOCS/인수인계_변경점_20260811.md` 를 근거 문서로 쓰고, upstream(`shain1912/Harness_Blockly`) 대비 PR 로 올리는 게 자연스럽다. `mslopgk/harness-blockly` 의 `master` 가 최신이다.
4. **키 재발급·교체.** `bundled-keys.cjs` 의 DeepSeek 키와 `blockpy-config.json` 의 키는 **내 개인 계정 키다.** 후임 계정 키로 반드시 바꿔라.
5. **LICENSE 파일 추가.** `package.json` 이 MIT 라고 선언하는데 파일이 없다.
6. **Dobot CDN 의존 제거 검토.** 온라인 설치본에서 제일 깨지기 쉬운 곳이다. 사내 미러를 두는 편이 낫다.
7. **`ir_desugar_app.spec.js` 플레이키 원인 규명.** 지금은 재실행으로 넘기고 있다.
8. **`_보관/` 처분 결정.** 3.3GB 다. §7 을 읽고 필요 없는 것만 지워라. **내가 판단해서 지우지 않았다.**

### 참고 — 2026-08-11 개선 8건 (무엇을 왜 고쳤는지)

발주 측 요구를 그대로 받은 것이다. 원문은 `DOCS/개선계약_20260811.md`.

1. 코드 마지막에 펌프가 항상 정지되게 → 모든 예제 끝에 `pump_off()`
2. TM 패널을 티처블머신과 판박이로 → `src/components/TeachableMachine.jsx` (692줄)
3. AI 도우미의 카메라 오지정 개선 → `/api/cameras`
4. 에이전트가 다음 단계를 물어 순차 진행 유도 → `agent-context/AGENTS.md`
5. 중등·고등 예제를 단계별로 분리 → `public/examples/` 15개 (찾기/잡기/놓기 식)
6. AI 도우미가 프로그램 실시간 상황을 알게 → `/api/ui-state`
7. TensorFlow 자동 설치 → 설치본이 `pip install tensorflow`
8. 보정값으로 어느 자리에 있어도 찾아가 집기 → `runtime/robotvision.py` `to_robot()`

---

## 7. `_보관/` 안내 — **지운 것은 하나도 없다**

인수인계 정리 때 "빌드 산출물·캐시·중복본·임시파일"을 `_보관/` 으로 **옮기기만** 했다. 원래 경로를 그대로 반영한 구조라서, 되살리려면 같은 상대 경로로 다시 옮기면 된다.

| `_보관/` 안의 경로 | 원래 있던 곳 | 무엇 / 되살려야 하나 |
|---|---|---|
| `플랫폼개발/blockly프로젝트/release` | 같은 상대 경로 | 637MB, 패키징 결과물. **불필요** — 다시 빌드하면 나온다 |
| `플랫폼개발/blockly프로젝트/dist` | 〃 | 40MB, Vite 빌드 산출물. **불필요** |
| `플랫폼개발/blockly프로젝트/test-results` | 〃 | Playwright 임시 결과. **불필요** |
| `플랫폼개발/blockly프로젝트/python-embed` | 〃 | 180MB, 오프라인 번들용 파이썬. 온라인 설치본으로 전환해서 **지금은 안 쓴다**(오프라인 설치본을 다시 만들려면 필요) |
| `플랫폼개발/blockly프로젝트/opencode-embed` | 〃 | 172MB, 위와 같음 |
| `플랫폼개발/blockly프로젝트/vendor-installers` | 〃 | 401MB, DobotLink 등 제3자 설치본. **오프라인 설치 시에만 필요.** 재배포 라이선스는 확인하지 않았다 (추정: 온라인 다운로드로 바꾼 이유 중 하나) |
| `강의자료개발/_백업_중등_20260728_1754` | 〃 | 173MB, 블록 전용 개편 **이전** 백업. 개편 전 상태를 보려면 필요. 그 외엔 불필요 |
| `루트/_전달_중등_20260811.zip` | 최상단 | 1.56GB. **바로 옆 `_전달_중등_20260811/` 폴더와 같은 내용**이다. 배포용 zip 이 필요하면 남기고, 아니면 지워도 된다 |
| `루트/_archive` | 최상단 | 178MB. `Dobot_Demo_V2.3`, `Magician go demos.rar`, `logic.v1.bak_분리수거_2026-07-07.html`, `제출물_검토용_납품본_2026-06-23`. **`제출물_검토용_납품본_2026-06-23` 은 납품 이력이라 지우지 말 것을 권한다** |
| `루트/image.png` | 최상단 | 출처 불명 낱장 이미지 (추정: 이슈 작성용 스크린샷) |

`_보관` 으로 옮긴 6개 폴더는 전부 git ignore 대상이었고, 옮긴 뒤 `blockly프로젝트` 작업트리에 변경 0건임을 확인했다. **즉 저장소 상태에 영향이 없다.**

옮기지 **않은** 큰 것: `플랫폼개발/커리큘럼코드/taskB_curriculum/models/` (1.5GB). 빌드 산출물이 아니라 실제 사용하는 VLA 모델 가중치라서 제자리에 뒀다(§2.2).
